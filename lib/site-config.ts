/** Marketing CTAs and display constants — single place for funnel consistency. */
export const SITE = {
  primaryCta: "Get Sample Leads",
  secondaryCta: "See How It Works",
  /** After search, when user still needs to pay */
  unlockCta: "Unlock lead pack ($49)",
  leadPackName: "Dentily High-Intent Lead Pack",
  leadPackPriceLabel: "$49",
  leadPackCount: 50,
} as const;
